# CLAUDE.md

사용자는 해당 프로젝트의 대해 잘 모름.
따라서 너가 알아서 잘 해줘야됨.
사용자는 윈도우와 맥을 연결해서 쓰려함.
알아서 잘 수행해줘야함.

# git관련
커밋이름은 한글로 몇단어 수준으로 짧아야됨.
커밋 생성 시 push까지 자동으로 함.
코드 작업을 마친 뒤 변경사항을 검토한다.

# 내부 메모리 필수 확인

- 모든 작업을 시작할 때 코드 조사, 수정, 명령 실행보다 먼저 이 저장소의 Claude Code 자동 메모리를 반드시 직접 확인한다.
- 자동으로 컨텍스트에 로드되었다고 가정하지 말고, 현재 Git 저장소에 대응하는 `~/.claude/projects/<project>/memory/MEMORY.md`를 도구로 읽는다.
- `MEMORY.md`에서 현재 요청과 관련된 키워드와 항목을 찾고, 연결된 주제별 메모리 파일이 있으면 관련 파일만 추가로 읽은 뒤 작업한다.
- 메모리에 기록된 기존 결정, 검증 명령, 알려진 문제, 사용자 선호를 현재 작업에 반영한다.
- 현재 사용자의 명시적 지시와 저장소의 실제 상태가 메모리보다 우선한다. 메모리가 없거나 접근할 수 없거나 오래되어 보이면 추측하지 말고 그 사실을 알린 뒤 현재 상태를 직접 확인한다.
- 메모리 확인을 생략한 상태로 작업을 완료했다고 보고하지 않는다.